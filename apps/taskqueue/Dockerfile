# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @tasks/taskqueue`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t tasks-taskqueue`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system tasks-taskqueue && \
          adduser --system -G tasks-taskqueue tasks-taskqueue

COPY apps/taskqueue/dist tasks-taskqueue/
COPY apps/taskqueue/package.json tasks-taskqueue/
RUN chown -R tasks-taskqueue:tasks-taskqueue .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix tasks-taskqueue --omit=dev -f install

CMD [ "node", "tasks-taskqueue" ]
