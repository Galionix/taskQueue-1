/* eslint-disable @typescript-eslint/no-var-requires */

describe('TelegramController (auto)', () => {
  it('should import the controller', () => {
    const ctrl = require('./telegram.controller');
    expect(ctrl).toBeDefined();
  });
});
